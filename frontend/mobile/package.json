{
  "name": "music-assistant-mobile-panel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild ./src/panel.js --bundle --format=esm --target=es2022 --minify --outfile=../../custom_components/music_assistant/mobile/panel.js && esbuild ./src/bubble.js --bundle --format=esm --target=es2022 --minify --outfile=../../custom_components/music_assistant/mobile/bubble.js"
  },
  "dependencies": {
    "lit": "3.3.3",
    "@sendspin/sendspin-js": "5.0.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
